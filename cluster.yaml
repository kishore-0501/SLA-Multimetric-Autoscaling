apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: sla-multimetric-cluster
  region: eu-west-1
  version: "1.34"

managedNodeGroups:
  - name: sla-workers
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 3
    volumeSize: 20
